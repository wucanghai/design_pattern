/**
 * @desc 用原型实例指向创建对象的类，使用于创建新的对象的类的共享原型的属性与方法。
 * @author patric patric 2023.02.07
 */
declare class Prototype {
    getSex(userSex: string): string;
    getAge(userAge: string): string;
}
export default Prototype;
